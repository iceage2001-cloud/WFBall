import React from "react";
import ReactDOM from "react-dom/client";
import WCQuizApp from "./WCQuizApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <WCQuizApp />
  </React.StrictMode>
);
